SELECT

device.category AS device,

COUNT(DISTINCT ecommerce.transaction_id) AS orders,

ROUND(SUM(items.item_revenue),2) AS revenue,

COUNT(DISTINCT user_pseudo_id) AS buyers,

ROUND(
    SUM(items.item_revenue) /
    SUM(SUM(items.item_revenue)) OVER() * 100,
2) AS revenue_contribution

FROM `bigquery-public-data.ga4_obfuscated_sample_ecommerce.events_*`,
UNNEST(items) AS items

WHERE event_name='purchase'

AND ecommerce.transaction_id IS NOT NULL
AND ecommerce.transaction_id!='(not set)'

GROUP BY device

ORDER BY revenue DESC;